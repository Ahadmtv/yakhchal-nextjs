# Nutrition data audit

تاریخ بررسی: ۱۱ مرداد ۱۴۰۵

## حکم انتشار

مجموعه `src/data/foods.ts` شامل ۷۷ خوراک است. هیچ ردیف دارای URL منبع، تاریخ بازبینی، تعریف دقیق دستور/برند یا وضعیت تأیید نیست. در نتیجه:

- تمام ردیف‌ها `sourceStatus: "unverified"`، `sourceUrl: null` و `reviewedAt: null` دارند.
- اعداد فقط برآورد عمومی به‌ازای ۱۰۰ گرم‌اند.
- صفحات SEO مستقل برای خوراک‌ها منتشر نمی‌شوند.
- رابط کاربر و صفحه روش‌شناسی این محدودیت را آشکار می‌کنند.
- داده نباید برای رژیم‌درمانی، محاسبه دارو یا تصمیم بالینی استفاده شود.

## ریسک‌های اصلی

- غذاهای ترکیبی مانند خورش، آش، ته‌چین و سالاد الویه با دستور و روغن متفاوت تغییر زیادی دارند.
- وزن پخته و خام، بخش خوراکی، میزان آب و روش پخت در رکوردها تعریف نشده است.
- اقلام بسته‌بندی‌شده مانند تن ماهی، دوغ و لبنیات به برند وابسته‌اند.
- اندازه‌های خانگی و تبدیل «یک سهم» به گرم وجود ندارد.
- فقط انرژی ثبت شده و درشت‌مغذی، فیبر، سدیم و ریزمغذی‌ها موجود نیست.

## فرمول فعلی

`calories = round(caloriesPer100g × weightInGrams / 100)`

ورودی وزن باید بزرگ‌تر از صفر و حداکثر ۵۰۰۰ گرم باشد. گردکردن فقط برای نمایش انجام می‌شود.

## دروازه تأیید هر ردیف

پیش از تغییر یک ردیف به `verified` باید این موارد ثبت شود:

1. منبع اولیه یا پایگاه داده معتبر با URL پایدار.
2. تاریخ دسترسی و نام نسخه/انتشار منبع.
3. حالت ماده: خام یا پخته، بخش خوراکی و واحد دقیق.
4. برای غذای ترکیبی، دستور مرجع، نسبت مواد و بازده وزن نهایی.
5. بازبینی مستقل عدد و واحد توسط فرد واجد صلاحیت.
6. ثبت دلیل هر تبدیل یا میانگین‌گیری.

برای اقلام برنددار، برچسب همان محصول بر میانگین عمومی اولویت دارد و باید نام برند/نسخه محصول ثبت شود.

## برنامه پیشنهادی پاک‌سازی

- مرحله ۱: مواد تک‌جزئی و کم‌ابهام مانند میوه‌ها، حبوبات پخته و تخم‌مرغ.
- مرحله ۲: اقلام برنددار با برچسب مشخص.
- مرحله ۳: غذاهای ترکیبی با دستور مرجع و محاسبه مواد اولیه.
- مرحله ۴: بازکردن صفحات مستقل فقط برای ردیف‌های تأییدشده و دارای متن تحریریه یکتا.

تا پایان این مراحل، noindex/404 صفحات جزئی غذای فعلی تصمیم درست محصول است.
