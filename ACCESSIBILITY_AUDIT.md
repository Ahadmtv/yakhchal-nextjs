# Accessibility audit

هدف: WCAG 2.2 AA برای مسیرهای اصلی. این سند نتیجه بررسی کد و کنترل خودکار است؛ آزمون با کاربر دارای فناوری کمکی همچنان لازم است.

Lighthouse موبایل روی build محلی production در ۱۱ مرداد ۱۴۰۵ امتیاز Accessibility برابر 100 و هیچ audit خودکار ناموفق ثبت نکرد.

## اصلاحات انجام‌شده

- زبان `fa` و جهت `rtl` در ریشه سند تعیین شده است.
- لینک پرش به محتوای اصلی و ناحیه `main` قابل فوکوس وجود دارد.
- منوی موبایل نام دسترس‌پذیر، `aria-modal`، بستن با Escape، focus trap و بازگشت فوکوس دارد.
- وضعیت مسیر فعال با `aria-current` اعلام می‌شود.
- هدف‌های تعاملی کوچک به حداقل ۴۴×۴۴ پیکسل افزایش یافته‌اند.
- ورودی وزن خطای متنی، `aria-invalid` و محدودیت عددی دارد.
- FAQها با `details/summary` و بدون وابستگی به JavaScript کار می‌کنند.
- جدول‌ها header واقعی دارند و در عرض کم قابل اسکرول‌اند.
- تصاویر تزئینی alt خالی و تصاویر محتوایی alt توصیفی دارند.
- `:focus-visible` و `prefers-reduced-motion` در CSS سراسری پشتیبانی می‌شوند.
- لینک‌های خارجیِ منابع که در تب جدید باز می‌شوند این رفتار را برای screen reader اعلام می‌کنند.

## کنترل دستی لازم پیش از انتشار

1. گردش کامل با Tab و Shift+Tab در عرض ۳۶۰، ۷۶۸ و ۱۴۴۰ پیکسل.
2. بررسی NVDA + Firefox و TalkBack + Chrome برای منو، محاسبه کالری و FAQ.
3. بزرگ‌نمایی ۲۰۰٪ و ۴۰۰٪ بدون از دست‌رفتن عملکرد یا اسکرول افقی صفحه.
4. سنجش contrast پس از هر تغییر رنگ یا تصویر.
5. بررسی اینکه نوار ثابت دانلود، آخرین کنترل صفحه را نپوشاند.

## محدودیت

امتیاز Lighthouse نشانه مفید است اما اثبات انطباق WCAG نیست. آزمون صفحه‌کلید، screen reader و بازبینی انسانی باید در چرخه انتشار بماند.
